
  # Screenshot Conversion Service

  This is a code bundle for Screenshot Conversion Service. The original project is available at https://www.figma.com/design/q219qlzNP5xmIX3UVS88rk/Screenshot-Conversion-Service.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  